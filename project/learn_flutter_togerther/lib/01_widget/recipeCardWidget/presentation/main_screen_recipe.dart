import 'package:flutter/material.dart';

class MainScreenRecipe extends StatelessWidget {
  const MainScreenRecipe({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Main Screen Recipe'),
      ),
    );
  }
}
